# dotfiles
Dotfiles

## Installation
First, ensure Stow is installed:

brew install stow
Then setup as follows:

git clone git@github.com/erwang01/dotfiles.git $HOME/dotfiles
cd $HOME/dotfiles
stow .
